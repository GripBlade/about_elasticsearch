# 应用服务 WEB 访问端口
server:
  port: 8080


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ess?serverTimezone=GMT%2B8
    username: root
    password: dch@0821
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 可以根据需要设置为 none, validate, update, create, create-drop
    show-sql: true # 设置为 true 以显示 SQL 查询
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml


